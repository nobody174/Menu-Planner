# Backlog Notes — 2026-06-28

Captured from user notes during the Phase 3 (referral/co-owner/data-isolation) testing session. **Analysis only — nothing here has been implemented.** Findings based on reading the current codebase, not assumptions.

---

### 1. Edit pantry functionality (add/remove UX)
- **Implemented?** Partially. `/api/pantry/add`, `/api/pantry/remove`, `/api/pantry` (GET) all exist and work server-side (`pi-deployment/flask_app.py:582-617`), backed by `core/household_paths.py` per-household storage. **No frontend calls any of these endpoints** — confirmed via grep, zero hits for `api/pantry` in `frontend/`. Pantry only surfaces today as a read-only `in_pantry` flag on shopping-list items.
- **Should implement?** Yes.
- **Verdict:** Good idea — this is a dead API with real backend support and no UI, i.e. low-effort, high-value gap. Almost a bug, not just an enhancement.
- **How:** Add a Pantry section (likely on Settings or its own page) with a text input + add button and a remove "x" per item, calling the existing endpoints. No backend work needed, just templates/JS.

### 2. Dynamic menu scaling for 2–6 persons
- **Implemented?** No. `core/menu_generator.py` only has `num_dinners` (how many meals/week); there is no concept of household size or per-person quantity scaling anywhere in the codebase (confirmed via grep for `persons`/`household_size`/`servings` — zero hits).
- **Should implement?** Maybe — real feature, not a quick win.
- **Verdict:** Needs discussion. Good idea long-term (every meal planner competitor has this), but it's a structural change: recipes need a base-serving count, shopping-list quantities need a scaling multiplier, and household needs a "default serving size" setting. Not a UI tweak.
- **How (if pursued):** Add `base_servings` to the Recipe model, add `default_servings` to Household, scale ingredient quantities in shopping-list generation by `desired_servings / base_servings`. Decide whether scaling is household-wide default or chosen per-week.

### 3. "What does HelloFresh cost?" — already researched
- **Implemented?** N/A (research task, not code). Already in memory: `pi-menu-pricing-data` — Hello Fresh Norway pricing (64.95kr/person/day ordinær, 45.45kr kampanje).
- **Action:** None needed — just flagging it's already answered and saved.

### 4. Realistic price for Menu Planner as a paid product
- **Implemented?** N/A — business/pricing decision, not code.
- **Should implement?** Needs discussion, not a coding task.
- **Verdict:** Worth a dedicated conversation once a paid tier's feature set is defined (the referral system was explicitly built "track now, reward later" with this in mind — see `menu-planner-referral-feature` memory). Any price point should be anchored against HelloFresh's ~45-65kr/person/day (item 3) but Menu Planner isn't delivering groceries, so it's a much smaller "convenience tool" price, likely a low monthly subscription (rough anchor: 20-50kr/month range), not a per-meal cost. Don't commit to a number without first deciding what's gated behind payment.

### 5. Multi-user support
- **Implemented?** Yes, largely. Household/role model (owner/editor/viewer/co-owner) plus Netflix-style profiles already exist and were the focus of Phases 2-3. If "multi-user" means "multiple households on one server," that's also implemented (each household is isolated — pending final verification per the in-progress data-isolation test). If it means something else (e.g., one recipe shared across unrelated households, public recipe marketplace), that's a different, unbuilt feature — clarify which is meant.
- **Should implement further?** Probably already covers the common case; revisit only if a specific multi-user scenario isn't covered.
- **Verdict:** Mostly done — needs a definition check, not new code.

### 6. Trial period: email/app login only, no SMS/phone contact
- **Implemented?** Yes, by default — there is no SMS/phone-number capture or messaging anywhere in the codebase (auth is email/password via `core/auth_helpers.py`). This is already the current behavior, not a to-do.
- **Should implement?** N/A — already true. If anything, this is a "don't add phone-based auth/notifications during the trial period" guardrail to remember, not a feature to build.
- **Verdict:** Good constraint to keep — worth noting as a decision so a future "let's add SMS 2FA" idea gets checked against this intent first.

### 7. Nobody can try the app without self-hosting — should this change?
- **Implemented?** Already changed, partially without anyone deciding it explicitly: signup is open and ungated in code (no invite-only flag, no `ALLOW_SIGNUP` check found), and Railway hosts a live public URL (`menu-planner-production-0b24.up.railway.app`). So in practice, self-hosting is *not* required today — anyone with the Railway link can sign up.
- **Should implement?** The code already allows it; the open question is really about *messaging/access*, not code: do you want the Railway URL to be the de facto public trial, and if so, do you want any guardrails (rate limiting signups, a "this is a beta" banner, abuse prevention)?
- **Verdict:** Good idea to formalize, low code risk since it already works — mainly needs a decision on hosting/cost limits (see item 9) and maybe basic abuse protection before actively promoting the link.

### 8. Rename `pi-deployment/` folder to `deployment/`
- **Implemented?** Not done. Folder name is a holdover from the original Raspberry Pi single-host design.
- **Should implement?** Yes, eventually — cosmetic but accurate.
- **Verdict:** Good idea, low risk, but **not zero-risk** — confirmed via grep that `pi-deployment` is referenced in `Dockerfile` (`ENV FLASK_APP=pi-deployment/flask_app.py`), `railway.json`-adjacent build process, and 5 GitHub Actions workflows (`ci.yml`, `lint.yml`, `tests.yml`, `security.yml`, plus `.github/scripts/verify_flask.py`). A rename means updating all of those in the same change, plus verifying Railway's build still finds the entry point. Mechanical, just not "rename one folder" — budget a full PR for it, not a quick edit.

### 9. Hosting strategy: Railway for early testing, Render for public production later
- **Implemented?** Railway is live today; Render is not set up.
- **Should implement?** Maybe — needs discussion, not urgent.
- **Verdict:** Reasonable strategy in principle (cheap/free tier for friends-and-family beta, dedicated production host once real users exist), but worth comparing Render vs. just scaling up Railway's plan before committing to a second platform — migrating hosts later means re-doing Postgres connection strings, env vars, and possibly the Dockerfile/`railway.json` build config again. Don't decide this until there's a concrete trigger (cost, limits, or a real production-readiness need), otherwise it's two platforms to maintain for no current benefit.

### 10. Simple way to add/remove pantry items (UI/UX)
- Duplicate of item 1 — same gap, same fix. Listed once in the roadmap to avoid double-tracking.

### 11. Real email confirmation during trial/signup
- **Implemented?** No. `is_valid_email()` only checks format — there is no confirmation email, no verification token, no "unconfirmed" account state. Any fake-looking address (e.g. `asdf@asdf.com`) is accepted immediately, while the password is still forced through full complexity rules (uppercase/lowercase/8+ chars) — a real inconsistency the user noticed directly while testing.
- **Should implement?** Yes, eventually — not urgent while there's no paid tier or real abuse problem, but worth doing before wider public promotion of the Railway link.
- **Verdict:** Good idea. Needs: a `email_verified` boolean + verification token on `User`, a confirmation email (see item 12 below for how to send it cheaply), and a decision on what happens to unconfirmed accounts after X days (freeze login, delete, or just nag — the user suggested "freeze after X days" as one option).
- **How:** Add `email_verified_at` (nullable) to `User`, generate a token on signup, send a confirmation link via the email provider chosen in item 12, gate login or key actions until verified (or just nag + auto-freeze after N days, per user's suggestion).

### 12. Send referral link via email/SMS directly from the app (research only, not started)
**Background:** users currently have to manually copy/paste the referral link to share it. The user asked: could the app instead take a friend's email or phone number and send the link for them — like the common "enter your email, get a copy" pattern seen on many sites? Two research passes were run; findings reconciled below (the second pass added more precision on exact free-tier limits).

**Email sending — cheap and low-risk to add soon:**
- **Resend** is the standout: 3,000 emails/month *and* 100/day, permanently free (current "New Free Tier," not a trial). Needs a verified sending domain (SPF/DKIM DNS records) to send to arbitrary recipients — a one-time setup cost that also improves deliverability for any other app email (password resets, etc.), so it's not feature-specific overhead. Official Python SDK + documented Flask integration guide exists.
- **Postmark** also has a genuinely permanent free tier, but capped hard at 100 emails/month with no overage — sending just stops once hit. Fine as a fallback, less headroom than Resend.
- **Ruled out:** Amazon SES (free tier is 12-months-only, not permanent, and sandbox mode blocks sending to unverified recipients — a dealbreaker since referral recipients are by definition unverified); SendGrid (permanent free plan discontinued as of 2025, now a 60-day trial only); Mailgun (free plan exists but limited — 1 domain, 1-day log retention); Gmail SMTP (free but throttled ~500/day with reputation-based flagging reported after as few as ~12 rapid sends, not intended for product/transactional use).
- `Flask-Mail` works via SMTP across most of these, so the integration pattern is reusable regardless of provider choice.
- **Verdict: worth doing soon.** Resend's free tier comfortably covers realistic referral volume at $0, setup is proportionate (domain DNS once + API key + a route + a template).

**SMS sending — defer until there's revenue:**
- No provider (Twilio, Vonage/Nexmo, AWS SNS/End User Messaging, Nordic specialist Link Mobility) has a meaningful free tier — every message costs money from message #1 (~$0.065/message to Norway via Twilio; AWS and Vonage broadly comparable for Nordic destinations).
- **Norway-specific landmine:** Norwegian carriers (Telenor, Telia, Ice) are documented to filter/block SMS containing URLs unless the sender ID and content are pre-allowlisted with the carrier through the provider — and the entire point of this feature is sending a clickable link. Link Mobility (the Nordic specialist most likely to handle this reliably) reportedly charges a one-time alphanumeric sender ID registration fee around €380+VAT covering all Norwegian operators — a real cost before the feature even works once.
- **Verdict: defer.** Zero free allowance, linear per-message cost, plus a non-trivial one-time setup fee to make links actually deliver — none of which is justified while there's no paid tier (app is still "track referrals now, reward later").

**Abuse/spam risk (applies even to the cheap email version):** letting a user type someone else's email or phone number and triggering an automated send to them is a well-known "send-to-a-friend" abuse vector (also usable for harassment or enumeration attacks). Needs, before shipping even the email-only version: rate limiting per sender/IP, CAPTCHA or honeypot as a backstop, sender must be logged-in/verified (ties abuse to an identifiable account), a fixed/templated message body (never user-controlled free text, to protect the sending domain's reputation), recipient opt-out/no-further-contact handling, and an audit trail (who sent what to whom, when — useful for abuse investigation and any GDPR/Norwegian Marketing Control Act questions, since the recipient's personal data is processed without their own consent at point of entry).

**How to implement (if/when prioritized):** start with Resend + Flask-Mail (SMTP) for email-only sending, add the abuse guardrails above before launch, and treat SMS as a separate, later decision gated on having a paid tier to justify the cost.

### 13. Landing/promo page before the signup form (for referral link clicks)
- **Implemented?** No. The referral link (`/signup?ref=CODE`) currently drops a brand-new visitor straight onto the bare signup form with zero context about what the product is.
- **Should implement?** Yes, eventually — not urgent, but a reasonable conversion improvement once there's something to show.
- **Verdict:** Good idea, low code risk, but gated on content (screenshot/clip), not code. The user is considering recording a short looping demo clip (e.g. via NVIDIA's overlay/Instant Replay capturing the focused window, then lightly trimmed) showing the categories tab or similar in action, rather than a longer narrated YouTube video — reasonable call for a referral-link audience that already has some trust from the person who shared it; a fuller narrated demo can be added later if needed.
- **How:** One new template (screenshot or muted autoplay/looping clip + 2-3 lines of copy + signup CTA that preserves `?ref=CODE`) and a small route change so `/signup?ref=CODE` lands here first, with the existing signup form one click away. Needs real media (clip/screenshot) before it's buildable — can be scaffolded with a placeholder now if useful.
- **Status (2026-06-28):** First recording made — `D:\Captures\Desktop\menu-planner_template.mp4` (35s, 9.72MB), a multi-feature montage (categories tab, recipe, shopping list, etc. in sequence). Decided: this clip is too long/varied for a tight autoplay loop, but works well as the **"full demo, click to play"** clip — keeping it for that purpose. A second, separate short clip is planned for the actual looping hook: just the menu page, the "Generate" button, and the categories tab — tight enough (~6-10s) to loop cleanly. So the page will likely end up with two pieces of media: a short loop near the top, and this longer demo further down or behind a "watch demo" link.
- **Built (2026-06-28):** Implemented. New route `/welcome` (`pi-deployment/flask_app.py`) + new template `frontend/templates/welcome.html`. `/signup?ref=CODE` now redirects to `/welcome?ref=CODE` first (unless `from_welcome=1` is set, which the welcome page's own CTA buttons append to avoid a redirect loop); plain `/signup` with no `ref` is untouched for organic/non-referral visits. Both clips copied into `frontend/static/media/` as `demo-loop.mp4` (the 6s loop, autoplay/muted/looping near the top) and `demo-full.mp4` (the 35s montage, click-to-play further down). Verified end-to-end locally: redirect chain, both video files serve at 200, ref param survives through to the real signup form.
- **User feedback after viewing live (2026-06-28):** Big improvement over the bare signup form. Needs some UI tuning/polish before going out publicly — not specified in detail yet, just flagged as "needs work." Decision: keep the current version as-is for now, through this testing phase — don't polish yet, revisit once broader testing is done and before any real public push of the referral link.

### 14. Theme flicker on button press (regenerate, etc.)
- **Reported by user (2026-06-28), not yet investigated.** Pressing the "Regenerate" button (or possibly any button that triggers a page action) causes a visible one-frame flash to a *different* theme before the page reloads into the correct destination and back to the user's actually-selected theme.
- **Implemented?** N/A — this is a bug report, not a feature.
- **Should fix?** Yes — visually jarring, looks broken even though the end state is correct.
- **Likely area to check (not yet confirmed):** theme CSS load order, FOUC (flash of unstyled content) from `theme-switcher.css` loading before the user's chosen theme CSS overrides it, or a default/fallback theme briefly rendering before a JS-driven theme application runs on page load.

### 15. Theme dropdown stuck open ("despawned") for two specific themes
- **Reported by user (2026-06-28), not yet investigated.** In Settings, the theme picker menu does not close properly for the **"Nordic Pantry"** and **"Pop Art Diner"** themes specifically — it gets stuck open/displayed ("despawned" — likely means the closing animation/logic doesn't fire, leaving it visually stuck).
- **Implemented?** N/A — bug report.
- **Should fix?** Yes — since it's only 2 of the themes (9 total per `FEATURE_ROADMAP.md`), likely a per-theme CSS/JS quirk rather than a general dropdown bug, otherwise all themes would show it.
- **Likely area to check (not yet confirmed):** theme-specific CSS files (`theme-nordic-pantry.css`, `theme-pop-art-diner.css` or similarly named) for a `z-index`, `display`, or `position` override that conflicts with the dropdown's own close behavior.

### 16. PIN code instead of full account password for the owner-profile-picker gate
- **Implemented?** No. Currently (`pi-deployment/flask_app.py:863-867`) re-selecting "Owner" from the profile picker reuses `authenticate_user()` — the same full email+password login check used at actual sign-in. The user wants a separate, short 4-digit PIN instead, since this gate exists specifically for shared-device/family use (switching back to Owner shouldn't require typing the real account password every time on a kitchen tablet, etc.).
- **Should implement?** Yes — reasonable UX improvement for the actual use case, and it was already flagged as "we talked about having a pin code" during this session, so it's an existing idea being revisited, not new scope.
- **Verdict:** Good idea, small-to-medium effort. Needs a real design decision on what happens if the PIN is forgotten (the account password is the obvious recovery path, so probably: PIN is optional, defaults to requiring full password if never set, and can always be reset via Settings using the real password as proof of identity).
- **How:** Add a `pin_code` (hashed, nullable) column to `User`. Add a "Set/Change PIN" control in `/settings` (owner-only, already locked down per item below). Update the profile-picker's owner-reselect check to: if a PIN is set, check the 4-digit PIN instead of the full password; if no PIN is set, fall back to the current full-password behavior. Store the PIN hashed (not plaintext) using the same hashing approach as `hash_password()` in `core/auth_helpers.py`, even though it's short — don't store it in clear text.

---

## Suggested phase placement
- **Near-term (next small PR):** #1/#10 (pantry UI — backend already exists, just wire it up), #12-email-only (Resend + Flask-Mail referral-send, cheap and low-effort).
- **Needs design decision first:** #2 (serving-size scaling), #8 (folder rename — needs a dedicated PR, not incidental), #11 (email confirmation — needs a decision on the unconfirmed-account freeze policy).
- **Business/strategy discussion (not code):** #3 (done), #4 (pricing), #6 (already true, just a guardrail to remember), #7 (already true in practice, decide on messaging/promotion), #9 (hosting — revisit when there's a concrete trigger).
- **Mostly done, verify scope:** #5 (multi-user — clarify which scenario is meant if anything still feels missing).
- **Deferred until revenue exists:** #12-SMS-only (no free tier, linear per-message cost, Norway URL-filtering complication).
- **Blocked on content, not code:** #13 (landing/promo page — needs a recorded clip or screenshot first).

This file is a snapshot for later triage — fold the relevant items into `FEATURE_ROADMAP.md` (which is itself known to be stale, see `menu-planner-data-isolation-gap` and the Phase 3 handoff doc) next time that doc gets a refresh pass.
