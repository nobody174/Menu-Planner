[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn --workers 4 --worker-class sync --bind 0.0.0.0:$PORT --timeout 120 --access-logfile - pi_deployment.flask_app:app"
healthcheckPath = "/health"
healthcheckTimeout = 30
healthcheckInterval = 10
