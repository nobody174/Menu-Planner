{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --workers 4 --worker-class sync --bind 0.0.0.0:5000 --timeout 120 --access-logfile - pi_deployment.flask_app:app",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
