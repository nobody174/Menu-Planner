# Backlog — Active

Last updated: 2026-07-02
See `CHANGELOG.md` for full history of completed work.

---

## BUGS / FIXES

### B12. Remove second confirmation popup after menu generation — DONE
- **Confirmed fixed:** Mobile testing, 2026-07-03 — only the initial "are you sure?" prompt remains

### B13. Settings → Household page overflows on mobile
- **Found:** Mobile testing, 2026-07-03
- **Current:** Member rows/text are too long for the viewport — text runs off screen, user has to horizontal-scroll to read full lines. Landscape was fine; portrait still overflowed after first fix attempt.
- **Fix attempt 1 (2026-07-03):** Stacked members table into cards + wrapped info-row text at 640px breakpoint — insufficient, portrait still overflowed
- **Fix attempt 2 (2026-07-03):** Widened breakpoint to 768px, added `overflow-x: hidden` safety net on html/body/container, stacked info-row label/value vertically, added `overflow-wrap: anywhere` broadly to table cells/spans
- **Status:** needs re-test on device in portrait
- **File:** `frontend/templates/household-settings.html`

### B14. Shopping list includes prep instructions instead of just the ingredient — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Cause:** Ingredient names in recipe data carry cutting/prep descriptors (e.g. "Gulrot, i skiver" / "Carrot, sliced", "Agurk (tynne skiver)"), which were passed straight through into the shopping list unparsed. Also meant e.g. "Gulrot" and "Gulrot, i skiver" deduplicated as separate lines instead of merging.
- **Fix (2026-07-03):** Added `strip_prep_descriptors()` in `core/ingredient_deduplicator.py` — regex-strips known Norwegian/English cutting/prep words from parenthetical `(...)` and trailing `, ...` suffixes (skiver, biter, terninger, hakket, sliced, diced, chopped, grated, etc.), applied before the dedup key is built (`deduplicate_ingredients`) and again when building the Norwegian name map in `deployment/flask_app.py:shopping_list()`
- **Files:** `core/ingredient_deduplicator.py`, `deployment/flask_app.py`
- **Status:** needs re-test. Note: only affects newly-generated shopping lists — an already-generated menu's cached shopping list won't retroactively update until the menu is regenerated.

### B15. Shopping list uses English unit "pcs" instead of Norwegian "stk" — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Cause:** `pcs` (the English unit string baked into recipe data) wasn't in the deduplicator's unit-normalization list, so it fell through unnormalized instead of becoming "pieces" like `stk`/`piece` did. Norwegian display also never translated the unit at all (only the ingredient name).
- **Fix (2026-07-03):** Added `pcs`/`pieces` to `UNIT_CONVERSIONS` and the primary-unit normalization check in `core/ingredient_deduplicator.py`; added unit translation (`pieces`/`pcs` → `stk`) alongside the existing name translation in `deployment/flask_app.py:shopping_list()`. Also added a space between quantity and unit in `shopping.html` (was rendering "2stk" with no gap).
- **Files:** `core/ingredient_deduplicator.py`, `deployment/flask_app.py`, `frontend/templates/shopping.html`
- **Status:** needs re-test

### B17. App requires relogin after installing as PWA / being away for a while — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Cause:** Flask session cookie was browser-session-only (no `PERMANENT_SESSION_LIFETIME`, `session.permanent` never set), so closing an installed PWA (common on mobile) dropped the session
- **Fix (2026-07-03):** Added `PERMANENT_SESSION_LIFETIME = 30 days` and `session.permanent = True` on both local and Azure login paths
- **File:** `deployment/flask_app.py`
- **Status:** needs re-test — install as PWA, leave idle a day, confirm still logged in

### B18. Settings (cogwheel) dropdown cut off at bottom on mobile — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Current:** Menu is taller than short phone viewports; bottom items (user info, logout) render off-screen with no way to scroll to them
- **Fix (2026-07-03):** Added `max-height: calc(100vh - 90px)` + `overflow-y: auto` to `.settings-dropdown` so it scrolls internally instead of overflowing the viewport
- **File:** `frontend/static/style.css`
- **Status:** needs re-test on device

### B19. PIN box auto-focused/auto-opened when entering household/settings on mobile — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Cause:** Both the PIN and password inputs in the owner-auth modal (`profile-picker.html`) carried the raw HTML `autofocus` attribute unconditionally, even though the modal itself is hidden (`display:none`) until the user clicks the Owner tile. Some mobile browsers still fire autofocus (and pop the on-screen keyboard) on a hidden input at page load. JS already existed (`focusOwnerAuthInput()`) to focus the input correctly only once the modal is actually shown — the static attribute was redundant and the buggy part.
- **Fix (2026-07-03):** Removed `autofocus` from both inputs; focus is now only ever set via JS when the modal is genuinely visible.
- **File:** `frontend/templates/profile-picker.html`
- **Status:** needs re-test on device

### B16. Auto-convert small ml quantities to dl in shopping list — FIXED
- **Found:** Mobile testing, 2026-07-03
- **Fix (2026-07-03):** In `core/ingredient_deduplicator.py:deduplicate_ingredients`, ml-based totals ≥100 and <1000 are now divided by 100 and shown as `dl` (300 ml → 3 dl); still `ml` under 100, still `l` at ≥1000.
- **Note:** this applies regardless of display language (no English-specific unit handling — English shopping lists will also show "dl", which isn't a standard US/UK unit; flag if that's undesired for the English toggle)
- **File:** `core/ingredient_deduplicator.py`
- **Status:** needs re-test

---

## FEATURES (planned, not started)

### F1. Themes — full rework
- Current themes are mostly colour swaps of the same layout
- Need real design-language themes: typography, spacing, card shapes, button styles
- CSS custom properties already in place
- Ideas: Dark mode, Newspaper/editorial, Minimalist/Japanese, Retro diner

### F2. Dessert + Drinks browsing system
- 90 dessert recipes stashed in `data/dessert-stash.json`
- 4 drink recipes stashed in `data/drinks-stash.json`
- Proposed UI: category grid → recipe list → detail page
- Blocker: no photos exist — recipes have no image field
- Decision: separate from dinner categories, not merged

### F3. Avatar upgrade — DiceBear API
- Current: static emoji picker
- Proposed: `https://api.dicebear.com/10.x/pixel-art/svg?seed={username}`
- No image storage needed, free, MIT licensed

### F5. "Around the World" recipe pack
- 1-2 iconic dishes per country/region, bilingual EN + NO
- `cuisine:xxx` tag system already in place — would slot in perfectly

### F6. Future ideas page
- 💡 icon in nav → page describing planned features
- Lets family/friends see what's coming and give early feedback

### F7. User delete own account — needs testing
- Danger zone section in Settings already built and deployed
- Needs end-to-end test: user + household + database all deleted cleanly

### F8. Sides stash feature
- 21 side dish recipes in `data/sides-stash.json`
- Future: "Side Dishes" section where users pick sides to go with weekly menu

### F9. Dessert system on dinner planner
- User picks which days they want dessert
- Generated from `dessert-stash.json`, separate from dinner recipes
- Shopping list gets a "Desserts" section
- Prerequisite: F2 browsing UI

---

## TESTING CHECKLIST

- [x] Delete own account — end to end (Settings → Danger Zone → confirm → deleted) — 2026-07-03: tested, working well
- [x] Forgot password flow — request, email arrives, click link, new password works — 2026-07-03: tested, working well
- [x] All 12 recipe packs import and generate 6-day menus correctly — 2026-07-03: tested, working well
- [x] Mobile test on menuplanner.no (pantry, generate menu, shopping list) — 2026-07-03: menu browsing, recipes, regenerate menu, and shopping list all work well on mobile. Settings/household does not (see B13) — may need "best on bigger screens" messaging, see note below.
- [ ] Second user joins household — sees same recipes and menu
- [x] Profile switching with PIN works correctly — 2026-07-03: tested, working well (see B19 for related PIN-focus mobile bug)
- [ ] Activity log shows correct entries after actions

### Mobile vs. desktop UX note
- **Found:** Mobile testing, 2026-07-03
- **Observation:** Core flows (menu, recipes, regenerate, shopping list) work well on phones. Settings/household management is cramped on small phone screens (see B13) — works fine on tablets/iPads/laptops/bigger phones.
- **Open question:** should the app explicitly communicate that Settings/Household is best used on a larger screen, or should it just be fixed to work on small phones too (B13)? Fixing responsiveness is likely the better long-term answer, but worth deciding.
