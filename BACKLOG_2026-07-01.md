# Backlog — 2026-07-01

Current open items only. Full history of completed work is in `BACKLOG_2026-06-30.md`.

---

## URGENT (time-sensitive)

### Railway trial expires ~2026-07-27 (~26 days)
- **Decision needed:** stay on Railway Hobby ($5/month), move to Render (free) + Neon (free), or Hetzner VPS (~€3.79/month)
- **Blocker before any migration:** per-household JSON files (`pantry.json`, `weekly_menu.json`, `categories.json` etc.) are stored on the Railway persistent volume — every other host has an ephemeral filesystem. Must migrate these into PostgreSQL (a `jsonb` column on households) before leaving Railway or all user data is lost on every deploy.
- **Research done:** full comparison in BACKLOG_2026-06-30.md F4 section.

---

## BUGS / FIXES

### B11. Recipe quality — minor instruction formatting issues FIXED (2026-07-01)
- 3 recipes had two-sentence steps: `tex_001` (Beef Tacos), `tex_002` (Chicken Enchiladas), `sum_027` (Poke Bowl)
- All split into single-action steps. `sum_027` had a missing "marinate for 10-15 minutes" instruction that was accidentally dropped in yesterday's cleanup — restored as a separate step.
- All other 272 recipes: 0 formatting issues found.

---

## FEATURES (planned, not started)

### F1. Themes — full rework
- Current themes are mostly colour swaps of the same layout
- Need real design-language themes: typography, spacing, card shapes, button styles — not just palette changes
- CSS custom properties already in place, just needs creative design work
- Ideas: Dark mode, Newspaper/editorial, Minimalist/Japanese, Retro diner

### F2. Dessert + Drinks browsing system
- 90 dessert recipes stashed in `data/dessert-stash.json` (full ingredients + instructions)
- 4 drink recipes stashed in `data/drinks-stash.json`
- Proposed UI: category grid → category list → detail page with large photo + recipe
- Real blocker: **no photos exist yet** — recipes have no image field at all
- Decision: separate category structure from dinner categories, not merged into the same list

### F3. Avatar upgrade
- Current: static emoji picker
- Proposed: DiceBear API — `https://api.dicebear.com/10.x/pixel-art/svg?seed={username}`
- No image storage needed, free, MIT licensed, CC0 styles available
- Simple to implement once prioritised

### F4. Hosting migration (see URGENT above)

### F5. "Around the World" recipe pack
- Curated pack with 1-2 iconic dishes per country/region
- Needs bilingual (EN + NO) recipe writing
- Depends on `cuisine:xxx` tag system already built — would slot in perfectly

### F6. Future ideas page
- A 💡 lightbulb icon in the nav linking to a simple page describing planned features
- Lets family/friends see what's coming and give early feedback
- Could be static HTML or read from a curated subset of this backlog

### F7. User delete own account
- Danger zone section in Settings already built and deployed
- Needs testing: confirm it deletes user + household + disk data cleanly

### F8. Sides stash feature
- 16 side dish recipes stashed in `data/sides-stash.json` (flatbread, mashed potatoes, etc.)
- Future idea: a "Side Dishes" section where users can browse/pick sides to go with their weekly menu
- Low priority, no design work done yet

### F9. Dessert system on the dinner planner
- User picks which days they want dessert
- Generated from `dessert-stash.json` pool, separate from dinner recipes
- Shopping list gets a separate "Desserts" section
- Prerequisite: F2 browsing UI

---

## TESTING (tomorrow morning)

- [ ] `www.menuplanner.no` — "not secure" warning should be gone (Cloudflare SSL)
- [ ] `noreply@menuplanner.no` → real signup → real confirmation email arrives in inbox
- [ ] Forgot password flow — request reset, email arrives, click link, set new password, log in
- [ ] Delete own account — Settings → Danger Zone → confirm password → account deleted
- [ ] Admin panel — log in as `nobodylearn174@gmail.com` → go to `/admin` → see all users → delete test user
- [ ] Verify all 12 recipe packs import correctly and generate a 6-day menu

---

## DONE (2026-06-29 / 2026-06-30 / 2026-07-01)

- Email confirmation system (signup → confirm → login gating)
- Password reset via email (forgot password flow)
- Delete own account (Settings danger zone, password confirmed)
- Admin panel (`/admin`, ADMIN_EMAIL gated, lists + deletes users)
- Unified favorites system across all pages
- Pantry items in collapsible "Already have these" section on shopping list
- Removed servings scaling (too confusing, users edit recipes directly)
- Fixed Railway volume freezing static seed data (SEED_DIR fix)
- Atomic writes for recipes_db.json (prevents corrupt JSON)
- Pack import now upserts (re-importing refreshes stale categories)
- 12 dish-type recipe packs live (replaced 5 geographic packs)
- 4 new Tex-Mex recipes written (Beef Tacos, Enchiladas, Nachos, Quesadillas)
- 90 desserts + 4 drinks + 16 sides removed from dinner pool and stashed
- Quick Dinners works as a real category (108 recipes tagged `quick`)
- Favorites + categories bug fixed (union instead of restrict)
- Pack-name pseudo-categories removed from all dropdowns
- Cuisine tags added to all recipes (101 tagged, 23 left as international/generic)
- Button sizing fixed globally (`btn-primary` no longer forces `width:100%`)
- Second "menu is ready" popup removed after regeneration
- menuplanner.no domain purchased, DNS configured via Cloudflare
- Resend email verified on menuplanner.no domain
- RESEND_API_KEY, RESEND_FROM_EMAIL, ADMIN_EMAIL all set on Railway
- Stale development docs removed from project root (12 files)
- Recipe instruction quality fixed: 3 recipes had multi-sentence steps, all corrected
