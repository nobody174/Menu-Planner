# Backlog — Active

Last updated: 2026-07-04
See `CHANGELOG.md` for full history of completed work.

---

## BUGS / FIXES

### B17. App requires relogin / intermittent "Oops!" errors — shipped, still monitoring
- **Found:** Mobile testing, 2026-07-03
- **Fix shipped:** Persistent 30-day sessions (`PERMANENT_SESSION_LIFETIME` + `session.permanent = True`) and `pool_pre_ping`/`pool_recycle` on the Postgres connection (a stale connection after Render idles out was the likely cause of the "Oops!" errors, and possibly of some session flakiness too)
- **Status:** no "Oops!" errors seen as of day 2 (2026-07-05) — promising, but keep watching a few more days before calling it fixed. If it recurs, the next thing to check is iOS WebKit's storage-purging behavior for standalone home-screen PWAs (a platform limitation, not something fixable server-side)

### B29. Settings dropdown renders off-screen when the cogwheel sits near the left edge — FIXED
- **Found:** User testing, 2026-07-05 — happens when the browser's unit/zoom scaling shifts the nav so the cogwheel lands on the left side of the screen
- **Cause:** `.settings-dropdown` is right-aligned to its button (`right: 0`) and expands leftward — fine when the button sits on the right side of the nav (the common case), but if the button ends up near the left edge, that leftward expansion runs off the left of the screen with no way to reach those items.
- **Fix (2026-07-05):** `toggleSettingsMenu()` in `frontend/static/app.js` now checks the dropdown's actual rendered position on open and clamps it to `left: 8px` if it would otherwise overflow off the left edge, same approach as the earlier vertical (B22) fix.
- **File:** `frontend/static/app.js`
- **Status:** needs re-test

### B28. Recipe eu_096 / eu_083 have broken combined-ingredient units — needs manual fix
- **Found:** during the B20 measurement-unit sweep, 2026-07-03
- **Issue:** Two recipes bundle multiple ingredients into a single line with one shared (and nonsensical) unit — not something a mechanical mapping can safely fix:
  - `pack_pasta_noodles.json` recipe `eu_096` — "Béchamel (melk, smør, mel)" has unit `ml/100g/50g`
  - `pack_beef_red_meat.json` recipe `eu_083` — "Smør og melk" has unit `gram/100ml`
- **Fix needed:** Split each into separate ingredient lines with correct individual amounts and units.

### Activity log may be silently dropping entries
- **Found:** while investigating B21 (swap-recipe), 2026-07-03
- **Issue:** Several places call `append_activity_to_db(household, ...)` using `household = current_household()`, which returns an already-detached ORM object from a closed database session — mutating a detached object doesn't persist anything since nothing commits it afterward.
- **Where:** `deployment/flask_app.py`, `current_household()` call sites around lines ~610, 629, 648, 945, 1004, 1018 — needs an audit of which of these need their own open-session-and-commit (like the swap-recipe fix in B21 got).
- **Why it matters:** matches the still-unchecked "Activity log shows correct entries after actions" item in the testing checklist below — some actions may never be logged.

---

## FEATURES (planned, not started)

### F11. Recipes don't indicate they're portioned for a family of 4
- **Requested:** User feedback, 2026-07-03
- **Idea:** Imported recipes assume a family of 4 as standard, but nothing on the page tells users this — they have to already know to self-adjust ingredient quantities for smaller/larger households. Add a visible note (recipe card, recipe detail page, or both).

### F1. Themes — full rework
- Current themes are mostly colour swaps of the same layout
- Need real design-language themes: typography, spacing, card shapes, button styles
- CSS custom properties already in place
- Ideas: Dark mode, Newspaper/editorial, Minimalist/Japanese, Retro diner

### F2. Dessert + Drinks browsing system
- 90 dessert recipes stashed in `data/dessert-stash.json`
- 4 drink recipes stashed in `data/drinks-stash.json`
- Proposed UI: category grid → recipe list → detail page
- Blocker: no photos exist — recipes have no image field
- Decision: separate from dinner categories, not merged

### F3. Avatar upgrade — DiceBear API
- Current: static emoji picker
- Proposed: `https://api.dicebear.com/10.x/pixel-art/svg?seed={username}`
- No image storage needed, free, MIT licensed

### F5. "Around the World" recipe pack
- 1-2 iconic dishes per country/region, bilingual EN + NO
- `cuisine:xxx` tag system already in place — would slot in perfectly

### F6. Future ideas page
- 💡 icon in nav → page describing planned features
- Lets family/friends see what's coming and give early feedback

### F8. Sides stash feature
- 21 side dish recipes in `data/sides-stash.json`
- Future: "Side Dishes" section where users pick sides to go with weekly menu

### F9. Dessert system on dinner planner
- User picks which days they want dessert
- Generated from `dessert-stash.json`, separate from dinner recipes
- Shopping list gets a "Desserts" section
- Prerequisite: F2 browsing UI

---

## TESTING CHECKLIST

- [x] Delete own account — end to end — 2026-07-03: tested, working well
- [x] Forgot password flow — 2026-07-03: tested, working well
- [x] All 12 recipe packs import and generate 6-day menus correctly — 2026-07-03: tested, working well
- [x] Mobile test on menuplanner.no (pantry, generate menu, shopping list, household/settings) — 2026-07-03/04: all confirmed working after fixes
- [x] Profile switching with PIN works correctly — 2026-07-03: tested, working well
- [x] Select number of days when generating a menu (F10) — 2026-07-04: tested, working well
- [x] Change day / delete recipe from inside recipe detail page (F12) — 2026-07-04: tested, working well
- [x] Two genuinely separate households (different accounts) each regenerate their own menu independently, no cross-contamination — 2026-07-05: tested, working well (vartdal@gmail.com + nobodylearn174@gmail.com, separate households)
- [x] Household-settings translations (B26) — 2026-07-05: confirmed "Add Family Profile" now shows in Norwegian
- [x] "Create Another Household" removal (B27) — 2026-07-05: confirmed gone
- [ ] Activity log shows correct entries after actions — still needs more testing, and may be broken (see the activity-log item above)
- [ ] Re-test after latest deploy: service worker fix (B25), recipe action button layout fix, new settings-dropdown horizontal clamp (B29)
